# AGENTS.md

Development repository for the Agentic Dev System.

## Context

- `.agents/AGENTS.md` — Agent context for the framework
- `.agents/README.md` — Usage guide
- `docs/agentic-dev-system-spec.md` — Full specification

The `workspace` folder contains temporary files. Load only on demand.

## Updating the System

- When the user asks to update the system, always update the spec file in `docs/agentic-dev-system-spec.md` to keep it up to date with the latest changes.
